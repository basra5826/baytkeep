/**
 * Tab action placeholder — tab press is handled in (tabs)/_layout.tsx.
 */

import { View } from 'react-native';

export default function AddPlaceTab() {
  return <View style={{ flex: 1 }} />;
}
